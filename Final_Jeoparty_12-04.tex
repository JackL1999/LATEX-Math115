\documentclass[12pt]{article}                         
\pagestyle{plain}

\usepackage{amsmath}     % Enhanced math environments (e.g., align).
\usepackage{amsfonts}    % Math fonts (e.g., \mathfrak{}).
\usepackage{amstext}     % Text inside math mode (e.g., \text{where}).
\usepackage{amssymb}     % Extra math symbols (e.g., \mathbb{R}).
\usepackage{array}       % Advanced table/array column definitions.
\usepackage{circledtext} % Puts text inside a circle (e.g., \circledtext{A}).
\usepackage{comment}     % Include/exclude blocks of text.
\usepackage{enumerate}   % Customize itemized/numbered lists.
\usepackage{geometry}    % Adjusts page margins and layout.
\usepackage{graphicx}    % Include images/graphics (\includegraphics).
\usepackage{latexsym}    % Access to basic LaTeX symbols.
\usepackage{multicol}    % Allows text columns on a page.
\usepackage{pgfplots}    % Create scientific plots from data (based on TikZ).
\usepackage{tabularx}    % Tables that stretch to page width.
\usepackage{tasks}       % Create multi-column lists.
\usepackage{textcomp}    % Provides many text symbols (e.g., \textcelsius).
\usepackage{tikz}        % Create vector graphics and diagrams.
\usepackage{xcolor}      % Define and use colors.
\usepackage{fancyhdr}
\usepackage{tcolorbox}
\usepackage{hyperref}

\geometry{a4paper, margin=1in}
\pagestyle{fancy}
\fancyhf{} % Clear all header and footer fields
\fancyhead[L]{Jack Lingbeck} % Left header with name
\fancyhead[R]{December 4th 2025} % Right header with date
\renewcommand{\headrulewidth}{0.4pt} % Horizontal line below the header

\begin{document}

% Main title
\begin{center}
    \Large \textbf{Math 115E Final Review} \\
    \vspace{0.2cm}
    \normalsize Jeopardy Planner \\
\end{center}
%LINK: \url{https://jeopardylabs.com/play/chapters-4-5-jeopardy-3} \\
%PASSWORD: : Math115
\normalsize
\subsection*{Activity 1 through Activity 9}
\begin{itemize}
    \item Types of numbers, integers, rationals, etc
    \item Interval Notation, Line vs $[,)$ vs $\leq, \geq$
    \item Intercepts from a gaph and their $(x,y)$ points, function table, fill in blanks
    \item Domain and Range from a graph, building their own
    \item Black out bingo, simple expressions to see who knows what from algebra
    \item Algebric Rules, simple comps $f(x), f(a), f(x^2+1)$, Foiling and pluggin in
    \item Algebraic Rules, harder comps $f(g(x)), f(x)g(x)$, etc
    \item Evaluating comps from table, graph, and function, 
    \item ARC on $[a,b]$, from function or graph
    \item Algebra Review of simple arthimatic
\end{itemize}

\subsection*{Activity 10 through Activity 17}
\begin{itemize}
    \item Star graph, slope from 2 points
    \item Star graph (blank), equation from 2 points, find value 
    \item Point slope form, slope intercept form, inequalities
    \item Finding two numbers puzzle $(a = 1, a \neq1 )$
    \item Standard form, vertex form, xy inters, vertex, min/max, factoring (a=1)
    \item Factoring when $a=1$ vs $a\neq1$
    \item Copied, quadratic formula
    \item wrap up, adding domain range, rectangle area puzzle
\end{itemize}

\subsection*{Activity 18 through Activity 21}
\begin{itemize}
    \item Transformation Rules, from function and graph, reflections too
    \item B4 Quiz Review, Graphing Quadratics, xy inters, vertex, sketch
    \item Polynomials, identifying them, degree, leading coeff, end behavior
    \item Multiplicities, xy-intercepts, bounce vs through from a graph, smallest degree
\end{itemize}

\subsection*{Algebraic Expressions}
\begin{itemize}
    \item[\$100]: Simplify $-2 \times -3 \times -4 \times -5 \longrightarrow 120$
    \item[\$200]: Simplify $2\cdot 7 + 3(2-12) + 6 \cdot 2 \longrightarrow -4$
    \item[\$300]: Simplify $\left(\displaystyle\frac{1}{2} + 1\right)^2 $
    \item[\$400]: Simplify $x^2 \cdot x^3 \cdot 3$
    \item[\$500]: Simplify $4^3 \div 4^2$
\end{itemize}

\subsection*{Linear Lines}
\begin{itemize}
    \item[\$100]: What is the form of a linear function? $\longrightarrow f(x) = mx+b$
    \item[\$200]: What is the slope of the function $f(x) = 5x-3 \longrightarrow m=5$
    \item[\$300]: Solve the linear expression: $12x+36 = -120 \longrightarrow x=-13$
    \item[\$400]: Given $f(x) = 4x - 10$, what is the average rate of change on $[-2,4] \longrightarrow 4$
    \item[\$500]: Solve the linear expression: $1 + 2(x-5) = x + 13 \longrightarrow x=22$
\end{itemize}

\subsection*{Rates of Change}
\begin{itemize}
    \item[\$100]: Find the slope between the points $(1,4)$ and $(5,20)\rightarrow m=4$
    \item[\$200]: Find the equation between the points $(3,-5)$ and $(1,-1) \rightarrow y=-2x+1$
    \item[\$300]: What $y$ value gives us a slope of $3$ from $(1,2)$ and $(2,y) \rightarrow y=5$
    \item[\$400]: What is the value of the slope for the line $y=10.75$
    \item[\$500]: What is the slope of the line $x=-2.5$
\end{itemize}

\normalsize
\subsection*{Quadratics}
\begin{itemize}
    \item[\$100]: Factor $x^2 + 2x + 1 \rightarrow (x+1)(x+1)$
    \item[\$200]: Factor $2x^2 + 5x + 3 \rightarrow (x+1)(2x+3)$
    \item[\$300]: Does $x^2 + 16x + 48 = (x+12)(x+4)$?
    \item[\$400]: Can $x^2 - 9x - 24$ be factored from the methods we learned? NO
    \item[\$500]: Find the solutions to $(x-2)(x+4)=-5$ with any method
    \end{itemize}

\subsection*{Properties}
\begin{itemize}
    \item[\$100]: What is the vertex of $-3x^2 + 2x + 6 \rightarrow (1/3, 19/3)$
    \item[\$200]: TRUE OR FALSE: The vertex alone tells us if that point is a max or min for a quadratic function. FALSE, need $a$
    \item[\$300]: What is the range for $x^2+1 \rightarrow (1,\infty)$
    \item[\$400]: What are the x-intercepts of $x^2-2x-1 \rightarrow (1\pm\sqrt{2},0)$
    \item[\$500]: How many real x-intercepts does $y=2x^2-4x+3$ have? $\rightarrow$ None
\end{itemize}

\subsection*{Function Stuffs}
\begin{itemize}
    \item[\$100]: Given $f(x) = 2x^2, g(x) = x - 3$, Find $f(x)g(x) \longrightarrow 2x^3-6x^2$
    \item[\$200]: Given $f(x) = 2x^2, g(x) = x - 3$, Find $f(g(x)) \longrightarrow 2x^2 - 12x + 18$
    \item[\$300]: Given $f(x) = 4x^2 - 2$, what is the average rate of change on $[0,2] \longrightarrow 8$
    \item[\$400]: Given $f(x) = -x^3 + 2x$ What is the average rate of change on $[-2,3] \longrightarrow -5$
    \item[\$500]:  Given $f(x) = 2x^2$. What will the function be with a transformation \\ right 2 units and down 3? $\longrightarrow g(x)=2(x-2)^2-3$
\end{itemize}

\subsection*{Polynomials}
\begin{itemize}
    \item[\$100]: TRUE OR FALSE: $f(x)= -x^2 + x^{-1}$ is a polynomial
    \item[\$200]: What is the leading coeff and degree of $g(x) = -2x^3 + 9x^2 -3 \longrightarrow $\text{coeff: -2, degree: 3}
    \item[\$300]: What is the end behavior for $f(x) = -x^2 + 2? \\
    \longrightarrow x \to \infty , f(x) \ to -\infty \text{ and }-x \to \infty , f(x) \ to -\infty$
    \item[\$400]: What are the multiplicities of each solution for $f(x) = (x+1)^2(x+2)\\
    \longrightarrow x=-1 (\text{mult 2}), x=-2 (\text{mult 1})$
    \item[\$500]: What are the multiplicities of each solution for $f(x) = x^2(x+1)^2(x+2)^3(x-4) \\
    \longrightarrow x=0 (\text{mult 2}), x=-1 (\text{mult 2}), x=-2 (\text{mult 3}), x=4 (\text{mult 1})$
\end{itemize}

\end{document}